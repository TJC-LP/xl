package com.tjclp.xl.ops

/** The seven laws under `FormulaSupport.textOnly` — the algebra without the formula engine. */
class EditLawsSpec extends EditLawsSuite(FormulaSupport.textOnly, "text-only")
